import ConfigPage from '../../platform/config/ConfigPage.jsx';

export default function AclPage() {
  return (
    <ConfigPage
      providerId="acl"
      title="ACL Rules"
      subtitle="Network access control lists — acl.conf.xml"
    />
  );
}
