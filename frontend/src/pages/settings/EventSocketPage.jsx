import ConfigPage from '../../platform/config/ConfigPage.jsx';

export default function EventSocketPage() {
  return (
    <ConfigPage
      providerId="event-socket"
      title="Event Socket"
      subtitle="ESL listener settings — event_socket.conf.xml"
    />
  );
}
